"""Tests for {{PROBLEM_NAME}}.

Rename this file to ``test_<slug>.py`` and drop it next to the solution
(e.g. ``practice/independent/<slug>/test_<slug>.py``) or in ``tests/``.
"""

from __future__ import annotations

import pytest

# from practice.independent.{{SLUG}}.solution import Solution


@pytest.mark.parametrize(
    "args, expected",
    [
        # ((input,), expected_output),
    ],
)
def test_solve(args, expected) -> None:  # type: ignore[no-untyped-def]
    # solution = Solution()
    # assert solution.solve(*args) == expected
    pytest.skip("Implement the solution and remove this skip.")
